BACKEND_URL=https://blockparty-backend-production.up.railway.app
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Pump.fun / PumpPortal settings
# Private key (base58) for the platform wallet to fund dev buy fallback (max 0.01 SOL)
PLATFORM_PRIVATE_KEY_BS58=
# Optional: RPC endpoint (defaults to a public Helius URL)
RPC_ENDPOINT=
# Optional: set to <= 0.01 for the platform fallback buy amount
PLATFORM_FALLBACK_MIN_SOL=0.01
# Optional: if your site runs behind a custom domain, set for image fetch
SITE_URL=
CREATOR_WALLET_PRIVATE=base58-encoded-private-key
SITE_URL=https://your-site-domain.example
